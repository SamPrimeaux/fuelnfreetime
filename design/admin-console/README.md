# Admin Console — Design Studio reference

Source export from Claude Design Studio (June 20, 2026).

| File | Purpose |
|------|---------|
| `Admin Console.dc.html` | Interactive prototype (dc-runtime + mock data) |
| `support.js` | Generated dc-runtime — not used in production |
| `uploads/` | Design session screenshots only |

Production admin lives in `public/admin/` — shell ported to `shell.js` + `css/console.css`.
